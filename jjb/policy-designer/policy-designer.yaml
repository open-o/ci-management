---
- project:
    name: policy-designer
    jobs:
      - '{project}-{stream}-verify-java'
      - '{project}-{stream}-merge-java'

    project: 'policy-designer'
    stream:
      - 'master':
          branch: 'master'
    mvn-settings: 'policy-designer-settings'