---
- project:
    name: common-services-auth
    jobs:
      - '{project}-{stream}-verify-java'
      - '{project}-{stream}-merge-java'

    project: 'common-services-auth'
    stream:
      - 'master':
          branch: 'master'
    mvn-settings: 'common-services-auth-settings'
