---
- project:
    name: sdno-lcm
    jobs:
      - '{project}-{stream}-verify-java'
      - '{project}-{stream}-merge-java'

    project: 'sdno-lcm'
    stream:
      - 'master':
          branch: 'master'
    mvn-settings: 'sdno-lcm-settings'
