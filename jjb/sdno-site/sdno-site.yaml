---
- project:
    name: sdno-site
    jobs:
      - '{project}-{stream}-verify-java'
      - '{project}-{stream}-merge-java'

    project: 'sdno-site'
    stream:
      - 'master':
          branch: 'master'
    mvn-settings: 'sdno-site-settings'
